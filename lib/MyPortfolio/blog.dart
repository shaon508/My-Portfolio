import 'package:flutter/cupertino.dart';

class BlogPage extends StatelessWidget {
  BlogPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is page is under construction!!!!"),
      ),
    );
  }
}